# Docs Sweeper Instructions

Review the todo app documentation.

Emit blocking findings if `README.md` does not document every supported command, `TODO_DB`, and how to run the Bats validation gate.

Write the required reviewer report JSON to `$BACH_AGENT_REPORT_PATH`.
