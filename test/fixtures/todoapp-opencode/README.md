# Todo App

This repository is intentionally incomplete. Run Bach to have OpenCode implement the real todo app:

```sh
bach run agent.build-todoapp
bach run agent.merge-todoapp
```

The acceptance gate is:

```sh
bach run shell.test
```
